<h1>Rennergebnis eingeben</h1>

<form method="POST" action="/race/save">
  <% drivers.forEach(d => { %>
    <div>
      <%= d.name %>
      <input name="<%= d.name %>" type="number" min="1" max="22" required>
    </div>
  <% }) %>

  <button>Speichern</button>
</form>